const fs_box = document.querySelector(".fs-box");
const post_received = document.querySelector(".post-received");

if (post_received != null && fs_box == null) {
        alert("재무 정보를 찾을 수 없습니다. 종목 이름을 확인해주세요.");
}